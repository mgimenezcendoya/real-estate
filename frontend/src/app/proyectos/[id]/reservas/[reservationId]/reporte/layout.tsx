// Intentionally minimal — detaches /reporte from the main app shell (no sidebar, no project tabs).
export default function ReporteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
